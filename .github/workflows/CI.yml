name: CI

on:
  release:
    types: [published]
  push:
    branches: [main, 0.x]
  pull_request:
    branches: [main, 0.x]
  workflow_dispatch:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install the project
        run: uv sync --all-extras --dev

      - name: Ruff
        run: uvx ruff check --exclude "datasets" --output-format=github .

      - name: Check format
        run: uv run ruff format --check .
